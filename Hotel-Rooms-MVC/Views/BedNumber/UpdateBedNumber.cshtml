@using Microsoft.AspNetCore.Mvc.TagHelpers
@model Hotel_Rooms_MVC.Models.ViewModel.bedNumberUpdateModel

<title>Update Bed Number </title>

<!-- Main Content -->
<div class="container">
    <div class="border">
        <h1>Update Bed Number</h1>
        <form asp-action="UpdateBedNumber"  >
            <div class="row">
                <div asp-validation-summary="All" class="text-danger"></div>
        
                <div class="col-md-6">
                    <!-- Left column fields -->
                    <div class="mb-4">
                        <input hidden asp-for="BedNumberUpdateDto.bedNo"/>
                        <label asp-for="BedNumberUpdateDto.bedNo" class="form-label fw-bold">Bed Number</label>
                        <input asp-for="BedNumberUpdateDto.bedNo" type="number" class="form-control" id="" disabled>
                    </div>
        
                    <div class="mb-4">
                        <label asp-for="BedNumberUpdateDto.RoomId" class="form-label fw-bold"></label>
                        <div class="input-group">
                            <select asp-for="BedNumberUpdateDto.RoomId" class="form-select" asp-items="@Model.RoomListItems">
                                <option disabled selected> # Select Room #</option>
                            </select>
                        </div>
                        <span asp-validation-for="BedNumberUpdateDto.RoomId" class="text-danger"></span>
                    </div>
                </div>
            </div>
            
        
            <div class="d-flex mt-5">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-plus-circle"></i> Update Bed's Room
                </button>
                <a asp-action="IndexBedNumber" asp-controller="BedNumber" class="btn btn-outline-primary ms-3">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>
        </form>
                        
        @section Scripts {
            <partial name="_ValidationScriptsPartial"/>
        }
    </div>
</div>


    