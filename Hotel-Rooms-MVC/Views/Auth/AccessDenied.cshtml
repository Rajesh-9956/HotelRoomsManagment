@{
    ViewData["Title"] = "Access Denied";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <div class="border room-card mt-5 text-center">
                <div class="room-card-header" style="background-color: var(--danger);">
                    <h2><i class="fas fa-ban"></i> Access Denied</h2>
                </div>
                <div class="room-card-body">
                    <p class="text-secondary">You don’t have permission to view this page.</p>
                    <a asp-controller="Room" asp-action="IndexRoom" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Back to Dashboard</a>
                </div>
            </div>
        </div>
    </div>
</div>